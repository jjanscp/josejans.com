---
import { profile } from '~/data/profile';
interface Props {
  size?: number;
}
const { size = 96 } = Astro.props;
---
<div
  class="avatar"
  style={`width:${size}px;height:${size}px;font-size:${size * 0.35}px;flex:0 0 ${size}px;`}
  aria-label={profile.name}
>
  {profile.initials}
</div>
